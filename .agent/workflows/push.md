---
description: 変更内容を Git にコミットし、GitHub へプッシュする
---

1. 変更されたファイルを確認する（`git status`）
2. 変更をステージングする（`git add .`）
3. 適切なコミットメッセージを作成してコミットする（`git commit -m "feat/fix: ..."`）
// turbo
4. リモートリポジトリへプッシュする（`git push origin main`）
